<Application x:Class="tamrin_6_2_final.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:tamrin_6_2_final"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style TargetType="TabItem" x:Key="tabItems">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border BorderThickness="0">
                            <WrapPanel>
                                <ContentPresenter>
                                    <TextBlock Text="{Binding Path=TabItem, ElementName=Header}" HorizontalAlignment="Center" VerticalAlignment="Center"></TextBlock>
                                </ContentPresenter>
                            </WrapPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
